package playercreation;

public enum PlayerQuestion {
    // Stores the types of questions that are asked of the user while creating a new Player at the start of the game.
    NAME,
    DESCRIPTION,
    SKILLTYPE,
    CONFIRM,
    RETURN,
}
