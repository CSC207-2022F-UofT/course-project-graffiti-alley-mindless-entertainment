package objects.battle.enemy.ai;

public interface EnemyAction {
    /**
     * This interface is implemented by the potential enemy's action such as skill and enemy potion
     * Enemy AI will decide the action and return Enemy Action, so it can avoid too many dependencies.
     */
}
